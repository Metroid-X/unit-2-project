<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css" type="text/css">
    <title>Editing Topic | <%= topic.title %> | <%=branchName%> | TotalRecall Forums</title>
</head>
<body>
    <main>
        <%- include('../partials/navbar.ejs') %>
        <div class="page-content">
            <h2>
                Editing <%= topic.title %>
            </h2>
            
            <div class="topic-builder-box">
                <form action="/<%=user.displayname%>/total-recall/<%=
                    String(branchName).toLowerCase()%>/<%=
                    (topic.title).replaceAll(' ', '-')%>/<%=
                    topic._id%>?_method=PUT" method="POST">
                    
                    <label for="title">Title: *</label>
                    <input type="text" name="title" id="title" value="<%=topic.title%>"/>

                    <label for="content">Content: *</label>
                    <textarea name="content" id="content"><%= topic.content %></textarea>

                    <label for="linked">Link an image:</label>
                    <input type="text" name="linked" id="linked" value="<%=topic.linked%>"/>

                    <button type="submit">Post</button>
                </form>
            </div>

        </div>
    </main>
</body>
</html>